spring:
  application:
    name: identity-service
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:sidequest-nacos}:8848
        file-extension: yaml
        group: DEFAULT_GROUP
      discovery:
        server-addr: ${NACOS_HOST:sidequest-nacos}:8848

